[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sages-stone"
version = "0.2.0"
description = "Constraint and emergence exploration engine"

[tool.setuptools]
packages = ["systems"]
